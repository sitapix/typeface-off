# Extra Google fonts to include beyond the popular auto-selected set.
# One family name per line, spelled exactly as on Google Fonts (e.g. "Space Grotesk").
# Lines starting with # and blank lines are ignored.
#
# After editing, run:  npm run fonts:generate
# The script looks each name up in the Google Fonts metadata (for its category +
# weights), verifies it exists on Bunny, then adds it — regardless of popularity.
# Names already in the catalog, listed twice, or misspelled are reported and skipped.

Onest
Pixelify Sans
